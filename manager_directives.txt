**CRITICAL PRIORITIES (SPRINT 1 REPAIR)**

**1. FIX TEST SUITE & PACKAGE NAMES**
- **Issue**: `tests` fail because of `ModuleNotFoundError: No module named 'mcp_servers'`.
- **Cause**: Directory is `mcp-servers` (hyphen) but validation/imports expect `mcp_servers` (underscore).
- **Action**: 
  - Rename `mcp-servers` directory to `mcp_servers`.
  - Update `docker-compose.yml` paths.
  - Ensure `__init__.py` exists in all packages.
  - RUN TESTS (`pytest`) locally to verify.

**3. LIVE VERIFICATION**
- Keys are now in `.env`.
- Ensure integration tests use `os.getenv` for credentials.
