**CRITICAL PRIORITIES (SPRINT 2: LOGIC LAYER)**

**1. IMPLEMENT AGENT ORCHESTRATION**
- **Goal**: Finish `backend/services/langgraph_agent.py`.
- **Action**:
  - Implement actual `_scout_node` to query Qdrant/Jira.
  - Implement `_execute_node` to call MCP tools (Jira/Slack).
  - Verify flow: `IntentRouter` -> `LangGraphAgent` -> `MCP`.

**2. VERIFY INTENT ROUTER**
- **Goal**: Ensure `backend/services/intent_router.py` correctly classifies:
  - "What is the status of JIRA-123?" -> TRIAGE/ACTION
  - "Create a ticket for..." -> ACTION
  - "How do I..." -> DIRECT_ANSWER

**3. INTEGRATION TEST**
- Access `http://localhost:3000` (Frontend) or use `curl` to tests `/api/chat`.
- Ensure responses flow back from the Agent.

